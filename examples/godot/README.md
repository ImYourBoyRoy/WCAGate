# Godot integration example

Use an automated scene test or instrumented debug build to inspect AccessibilityServer records, Control focus relationships, and interaction outcomes. Export the evidence contract and ingest it with `native-evidence` or `command-evidence`.

Do not infer Godot accessibility from `.tscn` text alone. Runtime roles, states, focus, actions, and platform announcements are the relevant artifacts.
